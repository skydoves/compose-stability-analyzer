class CardScope {
  constructor() /* primary */ {
    super/*Any*/()
    /* <init>() */

  }

}

data class User {
  val name: String
    field = name
    get

  val age: Int
    field = age
    get

  constructor(name: String, age: Int) /* primary */ {
    super/*Any*/()
    /* <init>() */

  }

  operator fun component1(): String {
    return <this>.#name
  }

  operator fun component2(): Int {
    return <this>.#age
  }

  fun copy(name: String = <this>.#name, age: Int = <this>.#age): User {
    return User(name = name, age = age)
  }

  override operator fun equals(other: Any?): Boolean {
    when {
      EQEQEQ(arg0 = <this>, arg1 = other) -> return true
    }
    when {
      other !is User -> return false
    }
    val tmp_0: User = other /*as User */
    when {
      EQEQ(arg0 = <this>.#name, arg1 = tmp_0.#name).not() -> return false
    }
    when {
      EQEQ(arg0 = <this>.#age, arg1 = tmp_0.#age).not() -> return false
    }
    return true
  }

  override fun hashCode(): Int {
    var result: Int = <this>.#name.hashCode()
    result = result.times(other = 31).plus(other = <this>.#age.hashCode())
    return result
  }

  override fun toString(): String {
    return "User(" + "name=" + <this>.#name + ", " + "age=" + <this>.#age + ")"
  }

}

@Composable
fun AutoTracedCard(user: User) {
  val _tracker: RecompositionTracker = rememberRecompositionTracker(composableName = "AutoTracedCard", tag = "", threshold = 2, fqName = "AutoTracedCard", isAutoTraced = true)
  _tracker.trackParameter(name = "user", type = "<root>.User", value = user, isStable = true)
  val _startTime: Long = nanoTime()
  try // COMPOSITE {
  println(message = "Rendering: " + user.<get-name>())
  // }
  finally // COMPOSITE {
  _tracker.recordDuration(startTimeNanos = _startTime)
  _tracker.logIfThresholdMet()
  // }
}

@Composable
fun CardWithDefaults(user: User, highlighted: Boolean = false) {
  val _tracker: RecompositionTracker = rememberRecompositionTracker(composableName = "CardWithDefaults", tag = "", threshold = 2, fqName = "CardWithDefaults", isAutoTraced = true)
  _tracker.trackParameter(name = "user", type = "<root>.User", value = user, isStable = true)
  _tracker.trackParameter(name = "highlighted", type = "kotlin.Boolean", value = highlighted, isStable = true)
  val _startTime: Long = nanoTime()
  try // COMPOSITE {
  println(message = "Rendering: " + user.<get-name>() + " (" + highlighted + ")")
  // }
  finally // COMPOSITE {
  _tracker.recordDuration(startTimeNanos = _startTime)
  _tracker.logIfThresholdMet()
  // }
}

@TraceRecomposition(tag = "explicit", threshold = 5)
@Composable
fun ExplicitlyTracedCard(user: User) {
  val _tracker: RecompositionTracker = rememberRecompositionTracker(composableName = "ExplicitlyTracedCard", tag = "explicit", threshold = 5, fqName = "ExplicitlyTracedCard", isAutoTraced = false)
  _tracker.trackParameter(name = "user", type = "<root>.User", value = user, isStable = true)
  val _startTime: Long = nanoTime()
  try // COMPOSITE {
  println(message = "Rendering: " + user.<get-name>())
  // }
  finally // COMPOSITE {
  _tracker.recordDuration(startTimeNanos = _startTime)
  _tracker.logIfThresholdMet()
  // }
}

@Composable
fun ExpressionBodyCard(user: User) {
  val _tracker: RecompositionTracker = rememberRecompositionTracker(composableName = "ExpressionBodyCard", tag = "", threshold = 2, fqName = "ExpressionBodyCard", isAutoTraced = true)
  _tracker.trackParameter(name = "user", type = "<root>.User", value = user, isStable = true)
  val _startTime: Long = nanoTime()
  try // COMPOSITE {
  return println(message = "Rendering: " + user.<get-name>())
  // }
  finally // COMPOSITE {
  _tracker.recordDuration(startTimeNanos = _startTime)
  _tracker.logIfThresholdMet()
  // }
}

@IgnoreStabilityReport
@Composable
fun IgnoredCard(user: User) {
  println(message = "Rendering: " + user.<get-name>())
}

@Composable
inline fun InlineWrapper(content: Function0<Unit>) {
  content.invoke()
}

@NonRestartableComposable
@Composable
fun NonRestartableCard(user: User) {
  println(message = "Rendering: " + user.<get-name>())
}

@Composable
fun CardScope.ScopedCard(user: User) {
  val _tracker: RecompositionTracker = rememberRecompositionTracker(composableName = "ScopedCard", tag = "", threshold = 2, fqName = "ScopedCard", isAutoTraced = true)
  _tracker.trackParameter(name = "user", type = "<root>.User", value = user, isStable = true)
  val _startTime: Long = nanoTime()
  try // COMPOSITE {
  println(message = "Rendering in scope: " + user.<get-name>())
  // }
  finally // COMPOSITE {
  _tracker.recordDuration(startTimeNanos = _startTime)
  _tracker.logIfThresholdMet()
  // }
}

@Composable
fun rememberLabel(user: User): String {
  return "label: " + user.<get-name>()
}

val currentLabel: String
  @ReadOnlyComposable
  @Composable
  get(): String {
    return "label"
  }

